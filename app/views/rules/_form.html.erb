<%= f.input :section, :as => :select, :collection => Rule.sections, :label => false %>
<%= f.input :part, :as => :select, :collection => Rule.parts, :label => false %>
<%= f.input :substance, :label => false %>
<%= f.submit "+", :name => "rule_plus" %>
<%= f.submit "-" %>
