<%= f.input :section, :as => :select, :collection => ["Subject", "From", "Body", "Date", "To", "Cc", "To or Cc", "X-Spam-level"] %>
<%= f.input :part, :as => :select, :collection => Rule::PARTS %>
<%= f.input :substance %>
<%= f.submit "+" %>
<%= f.submit "-" %>
