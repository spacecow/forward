<%= f.input :section, :as => :select, :collection => ["Subject", "From", "Body", "Date", "To", "Cc", "To or Cc", "X-Spam-level"], :label => false %>
<%= f.input :part, :as => :select, :collection => Rule::PARTS, :label => false %>
<%= f.input :substance, :label => false %>
<%= f.submit "+" %>
<%= f.submit "-" %>
